import {
  MenubarRoot as Root,
  type MenubarRootProps,
  type MenubarRootOptions,
} from "./menubar-root";
import {
  MenubarMenu as Menu,
  type MenubarMenuProps,
  type MenubarMenuOptions,
} from "./menubar-menu";

export type { MenubarRootOptions, MenubarRootProps };
export { Root };

export type { MenubarMenuOptions, MenubarMenuProps };
export { Menu };
