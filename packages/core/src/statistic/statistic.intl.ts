export const STATISTIC_INTL_TRANSLATIONS = {
	// Prefix of `Statistic.Trend`'s accessible text when the value increased.
	increase: "increased by",
	// Prefix of `Statistic.Trend`'s accessible text when the value decreased.
	decrease: "decreased by",
	// `Statistic.Trend`'s accessible text when the value did not change.
	noChange: "unchanged",
};

export type StatisticIntlTranslations = typeof STATISTIC_INTL_TRANSLATIONS;
