import { Preview, TabsSnippets } from "../../../components";
import { PopoverDemo } from "../../../examples/getting-started";

# Getting started

## Installation

Install Kobalte by running either of the following:

<TabsSnippets>
  <TabsSnippets.List>
    <TabsSnippets.Trigger value="npm">npm</TabsSnippets.Trigger>
    <TabsSnippets.Trigger value="yarn">yarn</TabsSnippets.Trigger>
    <TabsSnippets.Trigger value="pnpm">pnpm</TabsSnippets.Trigger>
  </TabsSnippets.List>
  <TabsSnippets.Content value="npm">
    {/* <!-- prettier-ignore-start --> */}
    ```bash
    npm install @kobalte/core
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
  <TabsSnippets.Content value="yarn">
    {/* <!-- prettier-ignore-start --> */}
    ```bash
    yarn add @kobalte/core
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
  <TabsSnippets.Content value="pnpm">
    {/* <!-- prettier-ignore-start --> */}
    ```bash
    pnpm add @kobalte/core
    ```
    {/* <!-- prettier-ignore-end --> */}
  </TabsSnippets.Content>
</TabsSnippets>

## Using the components

The example below demonstrate how to create a Popover component with Kobalte and styled with [TailwindCSS](https://tailwindcss.com/).

<Preview isCentered>
  <PopoverDemo />
</Preview>

```tsx
import { Popover } from "@kobalte/core";

export const PopoverDemo = () => (
  <Popover>
    <Popover.Trigger class="appearance-none outline-none h-10 px-4 rounded-md text-white bg-blue-600 dark:text-white/90 focus:ring focus:ring-blue-200 dark:focus:ring-blue-500/30">
      Learn more
    </Popover.Trigger>
    <Popover.Portal>
      <Popover.Positioner>
        <Popover.Content class="z-10 outline-none w-72 p-4 shadow-md border border-zinc-200 rounded-md bg-white dark:bg-zinc-800 dark:border-zinc-700">
          <Popover.Arrow />
          <p>
            Kobalte is a UI toolkit for building accessible web apps and design systems with
            SolidJS.
          </p>
        </Popover.Content>
      </Popover.Positioner>
    </Popover.Portal>
  </Popover>
);
```

In a few lines of code, we've implemented a fully accessible Popover component that :

- Adheres to [WAI-ARIA Dialog](https://www.w3.org/WAI/ARIA/apg/patterns/dialogmodal/) design pattern.
- Can be controlled or uncontrolled.
- Customize side, alignment, offsets.
- Optionally render a pointing arrow.
- Has focus fully managed and customizable.
